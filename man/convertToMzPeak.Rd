% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RmzPeak.R
\name{convertToMzPeak}
\alias{convertToMzPeak}
\title{Convert mzML Files to mzPeak Format}
\usage{
convertToMzPeak(path, pattern = ".mzML$|.mzml$", output_dir = "./")
}
\arguments{
\item{path}{\code{character(1)} Path to the directory containing the files to
convert.}

\item{pattern}{\code{character(1)} Regular expression used to match files within
\code{path}. Defaults to \code{".mzML$|.mzml$"}.}

\item{output_dir}{\code{character(1)} Path to the directory where the converted
\code{mzPeak} files will be saved. Defaults to the current directory.}
}
\description{
Converts one or more mzML files found in a directory into the mzPeak format.
This is a batch wrapper that identifies matching files by a regular
expression pattern, then converts each one individually.
}
\details{
The actual conversion is performed by the internal \emph{Rust} function
\code{mzpeak_convert()}, exposed to R via \emph{extendr}. For each input file,
\code{convertToMzPeak()} builds an output path (same basename, \code{.mzPeak}
extension, placed in \code{output_dir}) and passes both the input and output
paths to \code{mzpeak_convert()}, which:
\itemize{
\item Resolves the input filename and output path as native Rust \code{Path} \
\code{PathBuf} objects.
\item Performs the conversion using the \href{https://github.com/HUPO-PSI/mzPeak/blob/main/examples/convert.rs}{mzPeak example code}
with default parameter.
}

Because the conversion logic lives in compiled Rust code, any low-level I/O
or parsing errors are propagated back to R as an \code{io::Result} failure, which
is caught in \code{convertToMzPeak()} via \code{tryCatch} and re-thrown as an R error
with a message identifying the offending file.
}
\author{
Gabriele Tomè, Joshua Klein
}
